#ifndef FUNCA_H
#define FUNCA_H

/**
 * @brief Class that calculates the sum of the first n elements of the arctg(x) series.
 */
class FuncA {
public:
    /**
     * @brief Calculates the sum of the first n terms of the arctg(x) series.
     * 
     * @param n Number of terms to calculate.
     * @return double The sum of the series.
     */
    double calculateFuncA(int n);
};

#endif
